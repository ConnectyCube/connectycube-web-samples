export const credentials = {
  appId: REPLACE_APP_ID,
  authKey: 'REPLACE_APP_AUTH_KEY',
};

export const appConfig = {
  debug: {
    mode: 1,
  },
  chat: {
    streamManagement: {
      enable: true,
    },
  },
};